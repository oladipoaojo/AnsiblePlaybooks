---
- hosts: all
  become: yes
  SSH password: omoIKO@2015
  tasks:
    - name: ensure nginx is at the latest version
      apt: name=nginx state=latest update_cache=true
    - name: start nginx
      service:
          name: nginx
          state: started
